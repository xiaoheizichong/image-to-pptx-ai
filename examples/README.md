# Examples

Place sample images here, then run:

```bash
image-to-pptx examples/your-image.png -o output/your-image.pptx --mode local
```

The repository does not include binary sample images by default, so it stays lightweight.

